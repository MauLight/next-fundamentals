import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Customers'
}

export default function Page() {
    return (
        <div className="w-full min-h-screen flex justify-center items-center">
            <h1>Customers is in construction...</h1>
        </div>
    )
}